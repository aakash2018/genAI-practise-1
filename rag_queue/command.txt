docker compose up -d
venv environmwnt setup
.venv/Scripts/activate
pip freeze > requirements.txt

rq worker --worker-class rq.worker.SimpleWorker


export OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES

pip install -U langraph